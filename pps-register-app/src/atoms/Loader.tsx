import TadpoleLoader from '../assets/tadpoleLoader.svg?react';

const Loader = () => (
  <article className="flex items-center justify-center h-full w-full">
    <TadpoleLoader className="w-10 h-10 text-primary-500" />
  </article>
);

export default Loader;
